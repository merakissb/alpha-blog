<h1 class ="text-center mt-4 text-white"><%= @category.name %></h1>
<div class="text-center mt-4 text-white"> las mejores recetas de <%= @category.name %> </div>

<div class="text-center mt-4">
<% if logged_in? && current_user.admin? %>
  <%= link_to "Edit category name", edit_category_path(@category), class: "btn btn-light" %>
<% end %>
</div>
<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>

<%= render 'articles/article' %>

<div class="flickr_pagination mb-4">
  <%= will_paginate @articles, :container => false %>
</div>