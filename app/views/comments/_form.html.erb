<div class="container">    
  <div class="row justify-content-center">
    <div class="col-10 mt-4">
      <% if logged_in? %>
        <%= form_with(model: [ @article, @article.comments.build ], class: "shadow p-3 mb-3 bg-white rounded", local: true) do |form| %>
          <div class="form-group row">
            <%= form.label :Comentario, class: "col-2 col-form-label text-black" %>
            <div class="col-10">
              <%= form.text_area :body, class: "form-control shadow rounded", placeholder: "Escribe aquí tu comentario."%>
            </div>
          </div>
          <div class="form-group row justify-content-center">
            <%= form.submit class: "btn btn-dark" %>
          </div>
        <% end %>
        <% else %>
      <% end %>
    </div>
  </div>
</div>